package com.artknower.app.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Brand Color: #B85733 (Rich Rust Terracotta)
val RustTerracotta = Color(0xFFB85733)        // Main Accent & Primary Buttons
val RustTerracottaDark = Color(0xFF944122)    // Dark Rust for focus/containers
val RustTerracottaLight = Color(0xFFFAF0EC)   // Very soft rust highlight background

// Hero Gradient Accent (Rust → Warm Amber)
val HeroGradientStart = Color(0xFFB85733)     // Rust Terracotta
val HeroGradientEnd = Color(0xFFD4874A)       // Warm Amber
val HeroGradientAccent = Color(0xFFF2A96E)    // Soft peach highlight

// Clean Off-White & Card Surfaces
val CleanBackground = Color(0xFFFAF8F5)       // Soft Warm Ivory / White Background
val CardSurface = Color(0xFFFFFFFF)           // Pure White elevated cards
val WarmCardSurface = Color(0xFFF7F2EA)       // Soft warm tint cards
val ThinBorderColor = Color(0xFFEBE3D5)       // Delicate border
val CardShadowColor = Color(0x14231B15)       // Subtle warm shadow overlay

// Category / Pill Soft Tones
val PillTerracotta = Color(0xFFF9EDE8)
val PillGreen = Color(0xFFEAF4EC)
val TextGreen = Color(0xFF2E7D32)
val PillAmber = Color(0xFFFDF4E7)
val PillRose = Color(0xFFFCEFEB)
val PillSage = Color(0xFFEEF3F0)

// Text Tones
val DarkText = Color(0xFF231B15)              // Deep Warm Charcoal Text
val GrayText = Color(0xFF786C64)              // Muted Warm Gray Text
val MutedText = Color(0xFF64748B)             // Slate Muted Gray Text
val LightText = Color(0xFFFFFFFF)             // White text on primary buttons
val DeepNavy = Color(0xFF0F172A)              // Deep Navy for headers/accents

// Navigation
val NavSelectedPill = Color(0xFFFFEEE8)       // Soft terracotta tint for selected nav item

// Status accent colors
val StatusGreen = Color(0xFF16A34A)
val StatusAmber = Color(0xFFD97706)
val StatusRed = Color(0xFFDC2626)
val StatusBlue = Color(0xFF2563EB)
val StatusGreenBg = Color(0xFFDCFCE7)
val StatusAmberBg = Color(0xFFFEF3C7)
val StatusRedBg = Color(0xFFFEE2E2)
val StatusBlueBg = Color(0xFFDBEAFE)


